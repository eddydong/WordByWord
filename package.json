{
  "name": "word-by-word",
  "version": "1.0.0",
  "private": true,
  "description": "Dictation training webapp with Piper TTS via WebAssembly",
  "type": "module",
  "scripts": {
    "content:generate": "node ./scripts/generate-content-catalog.js",
    "predev": "npm run content:generate",
    "prebuild": "npm run content:generate",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "piper-timing-farm-browser": "^1.0.1"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
